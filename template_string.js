
// Template string, does not necessary concatenate strings.
function getValues(arr) {
   return `First position = ${arr[0]}, Second Position = ${arr[1]}`;
}

let x = [15, 25];

console.log(getValues(x));
